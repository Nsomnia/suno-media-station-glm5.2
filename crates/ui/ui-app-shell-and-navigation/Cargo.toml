[package]
name = "ui-app-shell-and-navigation"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true

[lints.rust]
unsafe_code = "deny"
missing_docs = "warn"

[dependencies]
design-tokens-theme-definitions = { path = "../../foundation/design-tokens-theme-definitions" }
ui-shared-widget-library = { path = "../ui-shared-widget-library" }
egui = { workspace = true }

[dev-dependencies]
# Re-used for tests that iterate every shipped theme.
design-tokens-theme-definitions = { path = "../../foundation/design-tokens-theme-definitions" }
