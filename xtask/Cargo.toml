[package]
name = "xtask"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true

[lints.rust]
unsafe_code = "deny"

[dependencies]
# Intentionally dependency-free (std only) — dev tooling must stay cheap to build.
