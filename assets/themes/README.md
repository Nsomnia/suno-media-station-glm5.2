# themes/

Token sources for the design-token system
(`crates/foundation/design-tokens-theme-definitions`): Catppuccin/Monokai
theme definitions as JSON/TOML, loaded at startup to build the token structs.

Populated in Phase 0 — see `docs/04-phase-roadmap.md`. Until then this
directory only carries `.gitkeep`.
